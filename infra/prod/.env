
# Caddy SSL Configuration
BACKEND_DOMAIN=localhost
CADDY_EMAIL=admin@example.com

# Server Configuration
NODE_ENV=development
SERVER_PORT=8000

# Database Configuration
POSTGRES_HOST=video-app-postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=video_app

# Redis Configuration
REDIS_PASSWORD=redis
REDIS_PORT=6379

# pgAdmin Configuration
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# JWT Configuration (use weak secrets for dev)
JWT_SECRET=dev-secret-key
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=dev-refresh-secret-key
JWT_REFRESH_EXPIRES_IN=30d

# Google OAuth Configuration
# Note: For mobile-only OAuth, the callback URL is not used
# The mobile app uses the SDK to get ID tokens directly
GOOGLE_CLIENT_ID=816917709083-lv7rhclh5jt9pa9c6al8666nbv1d49fp.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# CORS Configuration
# Allow all origins for mobile app requests (mobile apps don't have traditional origins)
CORS_ORIGIN=*

# Frontend URL (for redirects)
# Using production IP since backend is deployed
FRONTEND_URL=http://18.212.234.107
